<%- if devise_mapping.omniauthable? %>
  <%- resource_class.omniauth_providers.each do |provider| %>
    <%= form_for "Login", url: omniauth_authorize_path(resource_name, provider), method: :post, data: { turbo: "false" }, html: { class: "omniauth-form #{provider}-form" } do |f| %>
      <%= f.submit "Login", type: "image", src: asset_path("web_light_rd_SU@1x.png"), class: "omniauth-button #{provider}-login" %>
    <% end %>
  <% end %>
<% end %>
